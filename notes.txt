1. Clone the repository as .config into ~
   (or clone as config and then rename it)

2. Helix doesn't support the ~/.config folder. So use the following to set up a symlink.

   Run it from ~/AppData/Roaming

   mklink /d helix c:\users\mike\.config\helix
